import 'package:flutter/material.dart';

class BestsellingModel {
  final String foodSubtitle, foodTitle, image, rating, price;
  BestsellingModel(
      {@required this.foodSubtitle,
      @required this.foodTitle,
      @required this.image,
      @required this.price,
      @required this.rating});
}
